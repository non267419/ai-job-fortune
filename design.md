# AIお仕事占いメーカー 設計書

## 1. 目的

放課後等デイサービスのPC活動で、子どもたちがAIを使ってオリジナルの「お仕事占い」を作り、その場で友達に遊んでもらうためのWebアプリを作る。

このアプリの目的は、占いの正しさではなく、次の体験を支えること。

- AIに指示を出す
- AIの回答から使う内容を選ぶ
- 自分の作品として形にする
- 友達の作品を体験する

最優先の設計思想は、**小3でも迷わないシンプルさ、明確さ、簡単さ** とする。

## 2. 前提条件

- GitHub Pagesで公開する静的Webアプリとする
- Google Apps Scriptは使わない
- Googleスプレッドシートは使わない
- DB、サーバー、ログイン機能は使わない
- 管理画面は作らない
- 個人情報を入力させない
- 作った占いを全世界向けの一覧にはしない
- データは一定時間で消える

## 3. 基本方針

アプリ本体はGitHub Pages上に置くが、子どもが作った占いデータはサーバーへ送らない。

占いデータはブラウザ内に一時保存する。友達に渡すときは、サーバーに保存したIDではなく、占いデータそのものを共有コードに入れて渡す。

これにより、次を両立する。

- 友達の占いで遊べる
- インターネット上の公開一覧にはならない
- スプレッドシートやGASを使わない
- 一定時間でアプリ内から消える

## 4. 非対応とすること

今回のアプリでは、次は行わない。

- アカウント登録
- 本名登録
- サーバー保存
- 全端末で共有される占い一覧
- 職員用の管理画面
- コメント投稿
- いいね機能
- ランキング
- 作成者検索
- 画像アップロード

理由は、操作が複雑になり、個人情報や公開範囲のリスクが増えるため。

## 5. データの保存方針

### 5.1 保存場所

ブラウザの `sessionStorage` を使用する。

特徴:

- 同じタブを開いている間は残る
- ページ更新後も復元できる
- タブを閉じると消える
- サーバーには送られない

### 5.2 保存期限

占いデータには作成時刻と期限を持たせる。

- 初期設定: 60分
- 活動時間30分に対して余裕を持たせる
- 期限切れの占いは一覧から自動で消す
- 読み込んだ友達の占いも同じく60分で消す

### 5.3 注意点

これは強いセキュリティ機能ではなく、活動向けの一時保存設計である。

共有コードを写真に撮る、メモする、コピーすること自体は防げない。そのため、画面と活動説明で「本名や学校名は入れない」を徹底する。

## 6. 共有方針

### 6.1 共有方法

占い完成後、子どもは共有コードを友達に見せる。

- 共有コードを見せる

友達は「遊ぶ」画面で、共有コードを入力する。

### 6.2 共有データ

共有コードには、占いデータ本体を入れる。

サーバー上のIDは使わない。つまり、次のような方式にはしない。

- サーバーに保存する
- IDだけ共有する
- 友達がIDからサーバーに取りに行く

### 6.3 期限付き共有

共有コード内にも `expiresAt` を含める。

読み込み時に期限切れなら、次の表示にする。

> この占いは時間がすぎたので、もう遊べません。

## 7. 作成フロー

作成は「結果ファースト」にする。

理由:

- 最初にゴールが見える
- 子どもが完成形をイメージしやすい
- AIが出した結果タイプを先に選べる
- 質問作成が後から考えやすい

ただし、質問とタイプの細かいひもづけはさせない。複雑になるため。

### 7.1 作成手順

1. タイトルを入れる
2. ニックネームを入れる
3. 結果タイプを4つ決める
4. タイプごとの説明と向いている仕事を入れる
5. 質問を5こ入れる
6. ためしてみる
7. 今日の占いに入れる
8. 友達に見せる

### 7.2 子どもが決めること

- 占いタイトル
- ニックネーム
- タイプ名4つ
- 各タイプの説明
- 各タイプに向いている仕事
- はい/いいえで答えられる質問5こ

### 7.3 アプリが自動で行うこと

- 入力チェック
- 点数計算
- 結果の割り当て
- 一時保存
- 残り時間表示
- 期限切れ削除
- 共有コード生成

## 8. スコア設計

質問は5問固定とする。

- はい: 1点
- いいえ: 0点
- 合計: 0から5点

結果の割り当ては固定とする。

| 点数 | 表示する結果 |
| --- | --- |
| 0から1 | 結果1 |
| 2 | 結果2 |
| 3から4 | 結果3 |
| 5 | 結果4 |

子どもには「最小スコア」「最大スコア」を入力させない。

## 9. 画面構成

画面は大きく2つにする。

- 作る
- 遊ぶ

上部に大きなタブとして表示する。

### 9.1 作る画面

構成:

1. どんな占い？
2. どんなタイプが出る？
3. タイプの中身を入れよう
4. しつもんを5こ入れよう
5. ためしてみよう
6. 今日の占いに入れる

ボタン:

- ためしてみる
- 今日の占いに入れる
- 入れなおす

### 9.2 遊ぶ画面

構成:

1. 今日の占い一覧
2. 共有コードを入れる
3. 占いを選ぶ
4. 質問に答える
5. 結果を見る

ボタン:

- やってみる
- はい
- いいえ
- 結果を見る
- もう一回
- 今日の占いから消す

## 10. UI文言

小学生向けに、短く、具体的な言葉を使う。

使う言葉:

- 作る
- 遊ぶ
- 今日の占いに入れる
- 友達に見せる
- 共有コード
- やってみる
- はい
- いいえ
- 結果を見る
- あと何分
- ぜんぶ消す

避ける言葉:

- 登録
- 送信
- 投稿
- 保存
- データベース
- スコア範囲
- 管理
- 公開

## 11. 個人情報配慮

### 11.1 入力させない情報

- 本名
- 住所
- 学校名
- 電話番号
- メールアドレス
- 顔写真
- SNSアカウント

### 11.2 画面に出す注意

作る画面の上部に、短く表示する。

> 本名や学校名は入れないでね。ニックネームで作ろう。

### 11.3 入力欄の名称

「作者名」ではなく「ニックネーム」とする。

未入力でもよい。未入力の場合は「ななし」と表示する。

## 12. 入力チェック

保存前に、以下を確認する。

必須:

- タイトル
- 質問5こ
- タイプ名4つ

任意:

- ニックネーム
- タイプ説明
- 向いている仕事

文字数制限:

- ニックネーム: 12文字まで
- タイトル: 30文字まで
- 質問: 各60文字まで
- タイプ名: 各20文字まで
- 説明: 各80文字まで
- 向いている仕事: 各40文字まで

エラー文は短くする。

- タイトルを入れてね
- 質問を5こ入れてね
- タイプ名を4つ入れてね
- 文字が長すぎるよ

## 13. データ形式

アプリ内では、占い1件を次の形で扱う。

```js
{
  id: "local-abc123",
  title: "どうぶつお仕事占い",
  nickname: "りんご",
  createdAt: 1780218000000,
  expiresAt: 1780221600000,
  questions: [
    "みんなの前で話すのがすき？",
    "新しいアイデアを考えるのがすき？",
    "こまっている人を手伝いたい？",
    "何かを作るのがすき？",
    "最後まであきらめずにできる？"
  ],
  results: [
    {
      typeName: "お助けタイプ",
      description: "人の気持ちに気づくのが得意なタイプ。",
      jobs: "先生、看護師、カウンセラー"
    }
  ]
}
```

## 14. 期限切れ処理

次のタイミングで期限切れを確認する。

- アプリ起動時
- タブ切り替え時
- 占い一覧を表示するとき
- 共有コードを読み込むとき
- 1分ごとのタイマー

期限切れの占いは `sessionStorage` から削除する。

一覧には残り時間を表示する。

> あと42分

期限切れ直前は表示を変える。

> あと5分で消えるよ

## 15. 活動時の使い方

### 15.1 全体の流れ

| 時間 | 内容 |
| --- | --- |
| 5分 | 活動説明 |
| 10分 | AIで結果と質問を作る |
| 5分 | アプリに入れる |
| 5分 | 友達の占いを遊ぶ |
| 5分 | 感想共有 |

### 15.2 子どもの操作

1. AIで占いの結果タイプを作る
2. AIで質問を作る
3. アプリの「作る」に入力する
4. 「ためしてみる」で確認する
5. 「今日の占いに入れる」を押す
6. 友達に共有コードを見せる
7. 友達の占いを読み込んで遊ぶ

## 16. AIへの指示例

### 16.1 結果を先に作る

```text
小学生向けの「お仕事占い」を作ります。

結果タイプを4つ考えてください。
それぞれについて、
・タイプ名
・どんなタイプか
・向いている仕事3つ
を書いてください。

文章は小学校3年生にもわかるようにしてください。
```

### 16.2 質問を作る

```text
次の4つの結果が出る、お仕事占いの質問を作ってください。

・リーダータイプ
・アイデアタイプ
・お助けタイプ
・ものづくりタイプ

質問は5こです。
答えは「はい」「いいえ」にしてください。
小学校3年生にもわかる短い質問にしてください。
質問だけ出してください。
```

## 17. 実装ファイル構成

GitHub Pages向けに、最小構成とする。

```text
index.html
styles.css
app.js
README.md
```

外部CDNは使わない方針とする。理由は、活動中の通信失敗や外部サービス依存を減らすため。

## 18. 実装上の注意

- 画面読み込み時に期限切れデータを削除する
- 入力内容はHTMLとして表示せず、テキストとして表示する
- `innerHTML` の多用を避ける
- 共有コード読み込み時は形式チェックをする
- 不正な共有コードではエラーを出す
- 長すぎる共有コードは受け付けない
- 端末の時計に依存するため、厳密な期限管理ではない

## 19. 最終決定

このアプリは、**保存し続ける作品置き場** ではなく、**その日の活動の中だけで作品を作って遊ぶ道具** として設計する。

最終方針:

- GitHub Pagesのみで動く
- GAS、スプレッドシート、DBは使わない
- データは `sessionStorage` に一時保存
- 60分で自動削除
- 共有コードで友達に渡す
- 公開一覧は作らない
- 作成フローは結果ファースト
- 質問とタイプの細かいひもづけはさせない
- UIは「作る」「遊ぶ」の2つだけ
- 本名ではなくニックネームを使う
